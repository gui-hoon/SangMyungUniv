function [out] =  his(in) %in을 입력받아 out을 출력하는 함수 his [m,n] = size(in); % in의 행의 크기 = m, 열의 크기 = n in_max = max(max(in)); % in의 원소중 가장 큰값을 in_max에 저장 out = zeros(1,in_max+1); % 1 x (in_max+1) 크기의 행렬,0으로 초기화 % in의 전체 원소들을 탐색하며 각각의 원소값의 빈도수를 1증가 하여 out에 저장(히스토 그램화)  for i=1:m,          for j=1:n,      k = in(i,j);      out(k+1) = out(k+1)+1;     end  end   bar(out); % 막대그래프로 히스토그램 표시end    